# MVP UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a Flask-backed HTML/CSS/JS single-page app that lets users enter borefield sizing inputs and see L, H, NB results from `size_borefield()`.

**Architecture:** Flask serves one HTML page; the browser POSTs JSON to `/calculate`; Flask validates, calls `size_borefield()`, returns JSON; `main.js` renders the result in-place with no page reload.

**Tech Stack:** Python/Flask, plain HTML5, plain CSS3, vanilla JS (no frameworks).

## Global Constraints

- No inline styles in HTML — all styling goes in `static/style.css`
- No external CSS/JS frameworks (no Bootstrap, no jQuery)
- JSON field names in POST body match Python parameter names exactly: `q_h`, `q_m`, `q_y`, `k`, `alpha`, `T_g`, `Cp`, `mfls`, `T_in_HP`, `rbore`, `rpin`, `rpext`, `kgrout`, `kpipe`, `LU`, `hconv`, `B`, `NB`, `A`
- Flask app must be runnable from the project root with: `venv/bin/flask --app app run`
- Working directory for all commands: `/Users/agao/Downloads/CEE299/geosite_advisor`

---

## File Map

| File | Action | Responsibility |
|---|---|---|
| `requirements.txt` | Modify | Add `flask==3.1.1` |
| `app.py` | Create | Flask routes, input parsing, validation, calls `size_borefield` |
| `templates/index.html` | Create | Full page: sidebar, tab form, result panel |
| `static/style.css` | Create | All visual styling |
| `static/main.js` | Create | Tab switching, fetch POST, result/error rendering |

---

### Task 1: Install Flask and create `app.py`

**Files:**
- Modify: `requirements.txt`
- Create: `app.py`

**Interfaces:**
- Produces: `GET /` → `index.html`; `POST /calculate` → `{"L": int, "H": int, "NB": int}` or `{"error": "field"|"calculation", "field"?: str, "message": str}`

- [ ] **Step 1: Add Flask to requirements and install**

Add to `requirements.txt` (after the existing lines):
```
flask==3.1.1
```

Run:
```bash
venv/bin/pip install flask==3.1.1
```
Expected: `Successfully installed flask-3.1.1` (or "already satisfied")

- [ ] **Step 2: Create `app.py`**

```python
import sys, os
sys.path.insert(0, os.path.dirname(__file__))

from flask import Flask, render_template, request, jsonify
from geosite.s4_sizing.ashrae_sizing import size_borefield

app = Flask(__name__)


@app.route("/")
def index():
    return render_template("index.html")


@app.route("/calculate", methods=["POST"])
def calculate():
    data = request.get_json(force=True)

    # --- parse ---
    fields = ["q_h","q_m","q_y","k","alpha","T_g","Cp","mfls","T_in_HP",
              "rbore","rpin","rpext","kgrout","kpipe","LU","hconv","B","NB","A"]
    values = {}
    for f in fields:
        if f not in data or data[f] == "":
            return jsonify({"error":"field","field":f,
                            "message":f"'{f}' is required"}), 400
        try:
            values[f] = int(data[f]) if f == "NB" else float(data[f])
        except (ValueError, TypeError):
            return jsonify({"error":"field","field":f,
                            "message":f"'{f}' must be a number"}), 400

    # --- validate ranges ---
    if not (0.025 <= values["alpha"] <= 0.2):
        return jsonify({"error":"field","field":"alpha",
                        "message":"α must be between 0.025 and 0.2 m²/day"}), 400
    if not (0.05 <= values["rbore"] <= 0.1):
        return jsonify({"error":"field","field":"rbore",
                        "message":"Borehole radius must be between 0.05 and 0.1 m"}), 400
    if values["NB"] < 1:
        return jsonify({"error":"field","field":"NB",
                        "message":"Number of boreholes must be ≥ 1"}), 400
    if values["A"] < 1:
        return jsonify({"error":"field","field":"A",
                        "message":"Aspect ratio must be ≥ 1"}), 400

    # --- calculate ---
    try:
        L = size_borefield(
            q_h=values["q_h"], q_m=values["q_m"], q_y=values["q_y"],
            k=values["k"], alpha=values["alpha"], T_g=values["T_g"],
            Cp=values["Cp"], mfls=values["mfls"], T_in_HP=values["T_in_HP"],
            rbore=values["rbore"], rpin=values["rpin"], rpext=values["rpext"],
            kgrout=values["kgrout"], kpipe=values["kpipe"],
            LU=values["LU"], hconv=values["hconv"],
            B=values["B"], NB=values["NB"], A=values["A"],
        )
    except Exception as exc:
        return jsonify({"error":"calculation","message":str(exc)}), 500

    return jsonify({"L": round(L), "H": round(L / values["NB"]), "NB": values["NB"]})
```

- [ ] **Step 3: Smoke-test the backend**

Run in one terminal:
```bash
venv/bin/flask --app app run
```
In another terminal:
```bash
curl -s -X POST http://127.0.0.1:5000/calculate \
  -H "Content-Type: application/json" \
  -d '{"q_h":12000,"q_m":6000,"q_y":1500,"k":2.0,"alpha":0.086,"T_g":15,
       "Cp":4200,"mfls":0.05,"T_in_HP":40.2,"rbore":0.06,"rpin":0.01365,
       "rpext":0.0167,"kgrout":1.5,"kpipe":0.42,"LU":0.0511,"hconv":1000,
       "B":6.1,"NB":5,"A":1.0}'
```
Expected: `{"H": 30, "L": 152, "NB": 5}` (L≈152 is the known value for this input set)

- [ ] **Step 4: Test missing-field error**

```bash
curl -s -X POST http://127.0.0.1:5000/calculate \
  -H "Content-Type: application/json" \
  -d '{"q_h":12000}'
```
Expected: `{"error":"field","field":"q_m","message":"'q_m' is required"}`

- [ ] **Step 5: Test range error**

```bash
curl -s -X POST http://127.0.0.1:5000/calculate \
  -H "Content-Type: application/json" \
  -d '{"q_h":1,"q_m":1,"q_y":1,"k":2,"alpha":0.5,"T_g":15,"Cp":4200,
       "mfls":0.05,"T_in_HP":40,"rbore":0.06,"rpin":0.01365,"rpext":0.0167,
       "kgrout":1.5,"kpipe":0.42,"LU":0.0511,"hconv":1000,"B":6,"NB":5,"A":1}'
```
Expected: `{"error":"field","field":"alpha","message":"α must be between 0.025 and 0.2 m²/day"}`

- [ ] **Step 6: Commit**

```bash
git add requirements.txt app.py
git commit -m "feat: add Flask backend with /calculate route and validation"
```

---

### Task 2: Create `templates/index.html`

**Files:**
- Create: `templates/index.html`

**Interfaces:**
- Consumes: Flask `render_template("index.html")` from `app.py`
- Produces: DOM structure that `static/main.js` (Task 4) targets by these IDs/classes:
  - Tab buttons: `<button class="tab-btn" data-tab="basic">` and `data-tab="advanced"`
  - Tab panels: `<div id="tab-basic" class="tab-panel">` and `id="tab-advanced"`
  - All inputs: `<input name="q_h">` etc. (names match JSON field names)
  - Calculate button: `<button id="calc-btn">`
  - Result panel: `<div id="result-panel">` (hidden initially via CSS class `hidden`)
  - Result values: `<span id="res-L">`, `<span id="res-H">`, `<span id="res-NB">`
  - Calc error banner: `<div id="calc-error" class="hidden">`
  - Per-field error: `<span class="field-error" data-for="q_h">` etc.

- [ ] **Step 1: Create `templates/` directory and `index.html`**

```bash
mkdir -p templates
```

Create `templates/index.html`:

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>GeoSite Advisor</title>
  <link rel="stylesheet" href="/static/style.css">
</head>
<body>
  <div class="layout">

    <!-- ── Sidebar ── -->
    <aside class="sidebar">
      <div class="sidebar-title">GeoSite Advisor</div>
      <nav>
        <ul class="stage-list">
          <li class="stage-item disabled">s1 <span class="stage-name">Site</span><span class="badge">soon</span></li>
          <li class="stage-item disabled">s2 <span class="stage-name">Simulation</span><span class="badge">soon</span></li>
          <li class="stage-item disabled">s3 <span class="stage-name">Loads</span><span class="badge">soon</span></li>
          <li class="stage-item active"><a href="#">s4 <span class="stage-name">Sizing</span></a></li>
          <li class="stage-item disabled">s5 <span class="stage-name">Cost</span><span class="badge">soon</span></li>
          <li class="stage-item disabled">s6 <span class="stage-name">Strategy</span><span class="badge">soon</span></li>
          <li class="stage-item disabled">s7 <span class="stage-name">Report</span><span class="badge">soon</span></li>
        </ul>
      </nav>
    </aside>

    <!-- ── Main ── -->
    <main class="main">
      <h1 class="page-title">Borefield Sizing — Stage 4</h1>

      <!-- Tabs -->
      <div class="tab-strip">
        <button class="tab-btn active" data-tab="basic">Basic</button>
        <button class="tab-btn" data-tab="advanced">Advanced</button>
      </div>

      <form id="sizing-form" novalidate>

        <!-- ── Basic Tab ── -->
        <div id="tab-basic" class="tab-panel">

          <!-- Ground Loads -->
          <div class="card">
            <h2 class="card-title">Ground Loads</h2>
            <p class="hint">Negative = heat extraction (heating mode). Positive = heat injection (cooling mode).</p>
            <div class="field">
              <label for="q_h">Peak hourly load</label>
              <div class="input-row">
                <input type="number" id="q_h" name="q_h" placeholder="-392250">
                <span class="unit">W</span>
              </div>
              <span class="field-error" data-for="q_h"></span>
            </div>
            <div class="field">
              <label for="q_m">Peak monthly load</label>
              <div class="input-row">
                <input type="number" id="q_m" name="q_m" placeholder="-100000">
                <span class="unit">W</span>
              </div>
              <span class="field-error" data-for="q_m"></span>
            </div>
            <div class="field">
              <label for="q_y">Annual average load</label>
              <div class="input-row">
                <input type="number" id="q_y" name="q_y" placeholder="-1762">
                <span class="unit">W</span>
              </div>
              <span class="field-error" data-for="q_y"></span>
            </div>
          </div>

          <!-- Ground Properties -->
          <div class="card">
            <h2 class="card-title">Ground Properties</h2>
            <div class="field">
              <label for="k">Thermal conductivity</label>
              <div class="input-row">
                <input type="number" id="k" name="k" step="0.01" placeholder="2.0">
                <span class="unit">W/m·K</span>
              </div>
              <span class="field-error" data-for="k"></span>
            </div>
            <div class="field">
              <label for="alpha">Thermal diffusivity <em>(0.025 – 0.2)</em></label>
              <div class="input-row">
                <input type="number" id="alpha" name="alpha" step="0.001" placeholder="0.086">
                <span class="unit">m²/day</span>
              </div>
              <span class="field-error" data-for="alpha"></span>
            </div>
            <div class="field">
              <label for="T_g">Undisturbed ground temp</label>
              <div class="input-row">
                <input type="number" id="T_g" name="T_g" step="0.1" placeholder="15">
                <span class="unit">°C</span>
              </div>
              <span class="field-error" data-for="T_g"></span>
            </div>
          </div>

          <!-- Borefield -->
          <div class="card">
            <h2 class="card-title">Borefield</h2>
            <div class="field">
              <label for="B">Borehole spacing</label>
              <div class="input-row">
                <input type="number" id="B" name="B" step="0.1" placeholder="6.1">
                <span class="unit">m</span>
              </div>
              <span class="field-error" data-for="B"></span>
            </div>
            <div class="field">
              <label for="NB">Number of boreholes</label>
              <div class="input-row">
                <input type="number" id="NB" name="NB" step="1" min="1" placeholder="32">
                <span class="unit">—</span>
              </div>
              <span class="field-error" data-for="NB"></span>
            </div>
            <div class="field">
              <label for="A">Aspect ratio <em>(≥ 1)</em></label>
              <div class="input-row">
                <input type="number" id="A" name="A" step="0.1" min="1" placeholder="1.0">
                <span class="unit">—</span>
              </div>
              <span class="field-error" data-for="A"></span>
            </div>
            <div class="field">
              <label for="T_in_HP">HP inlet temperature</label>
              <div class="input-row">
                <input type="number" id="T_in_HP" name="T_in_HP" step="0.1" placeholder="4.44">
                <span class="unit">°C</span>
              </div>
              <span class="field-error" data-for="T_in_HP"></span>
            </div>
            <div class="field">
              <label for="mfls">Flow rate per kW</label>
              <div class="input-row">
                <input type="number" id="mfls" name="mfls" step="0.001" placeholder="0.05">
                <span class="unit">kg/s·kW</span>
              </div>
              <span class="field-error" data-for="mfls"></span>
            </div>
          </div>

        </div><!-- /tab-basic -->

        <!-- ── Advanced Tab ── -->
        <div id="tab-advanced" class="tab-panel hidden">
          <div class="card">
            <h2 class="card-title">Borehole &amp; Pipe Details</h2>
            <div class="field">
              <label for="rbore">Borehole radius <em>(0.05 – 0.1)</em></label>
              <div class="input-row">
                <input type="number" id="rbore" name="rbore" step="0.001" value="0.06">
                <span class="unit">m</span>
              </div>
              <span class="field-error" data-for="rbore"></span>
            </div>
            <div class="field">
              <label for="rpin">Pipe inner radius</label>
              <div class="input-row">
                <input type="number" id="rpin" name="rpin" step="0.00001" value="0.01365">
                <span class="unit">m</span>
              </div>
              <span class="field-error" data-for="rpin"></span>
            </div>
            <div class="field">
              <label for="rpext">Pipe outer radius</label>
              <div class="input-row">
                <input type="number" id="rpext" name="rpext" step="0.00001" value="0.0167">
                <span class="unit">m</span>
              </div>
              <span class="field-error" data-for="rpext"></span>
            </div>
            <div class="field">
              <label for="kgrout">Grout conductivity</label>
              <div class="input-row">
                <input type="number" id="kgrout" name="kgrout" step="0.01" value="1.5">
                <span class="unit">W/m·K</span>
              </div>
              <span class="field-error" data-for="kgrout"></span>
            </div>
            <div class="field">
              <label for="kpipe">Pipe conductivity</label>
              <div class="input-row">
                <input type="number" id="kpipe" name="kpipe" step="0.01" value="0.42">
                <span class="unit">W/m·K</span>
              </div>
              <span class="field-error" data-for="kpipe"></span>
            </div>
            <div class="field">
              <label for="LU">U-tube spacing</label>
              <div class="input-row">
                <input type="number" id="LU" name="LU" step="0.0001" value="0.0511">
                <span class="unit">m</span>
              </div>
              <span class="field-error" data-for="LU"></span>
            </div>
            <div class="field">
              <label for="hconv">Convection coefficient</label>
              <div class="input-row">
                <input type="number" id="hconv" name="hconv" step="1" value="1000">
                <span class="unit">W/m²·K</span>
              </div>
              <span class="field-error" data-for="hconv"></span>
            </div>
            <div class="field">
              <label for="Cp">Fluid heat capacity</label>
              <div class="input-row">
                <input type="number" id="Cp" name="Cp" step="1" value="4200">
                <span class="unit">J/kg·K</span>
              </div>
              <span class="field-error" data-for="Cp"></span>
            </div>
          </div>
        </div><!-- /tab-advanced -->

        <!-- Calculate -->
        <div id="calc-error" class="calc-error hidden"></div>
        <button type="button" id="calc-btn">Calculate</button>

      </form>

      <!-- Result Panel -->
      <div id="result-panel" class="result-panel hidden">
        <h2 class="result-title">Results</h2>
        <div class="result-grid">
          <div class="result-item">
            <span class="result-label">Total borefield length L</span>
            <span class="result-value"><span id="res-L">—</span> m</span>
          </div>
          <div class="result-item">
            <span class="result-label">Borehole depth H</span>
            <span class="result-value"><span id="res-H">—</span> m</span>
          </div>
          <div class="result-item">
            <span class="result-label">Number of boreholes</span>
            <span class="result-value"><span id="res-NB">—</span></span>
          </div>
        </div>
      </div>

    </main>
  </div>
  <script src="/static/main.js"></script>
</body>
</html>
```

- [ ] **Step 2: Verify Flask can serve it**

With Flask still running:
```bash
curl -s http://127.0.0.1:5000/ | grep "GeoSite Advisor"
```
Expected: `<title>GeoSite Advisor</title>` appears in output.

- [ ] **Step 3: Commit**

```bash
git add templates/index.html
git commit -m "feat: add HTML page layout with sidebar, tabs, form, result panel"
```

---

### Task 3: Create `static/style.css`

**Files:**
- Create: `static/style.css`

**Interfaces:**
- Consumes: class/id names defined in `templates/index.html` (Task 2)
- Produces: visual layout — no behavioural changes

- [ ] **Step 1: Create `static/` directory and `style.css`**

```bash
mkdir -p static
```

Create `static/style.css`:

```css
/* ── Variables ── */
:root {
  --sidebar-bg: #1e2a38;
  --sidebar-text: #b0bec5;
  --sidebar-active-bg: #2e7d5e;
  --sidebar-active-text: #ffffff;
  --accent: #2e7d5e;
  --bg: #f4f6f8;
  --card-bg: #ffffff;
  --border: #dde1e7;
  --text: #1a202c;
  --text-muted: #6b7280;
  --unit-color: #9ca3af;
  --error-color: #dc2626;
  --result-bg: #f0faf5;
  --result-border: #6ee7b7;
  --tab-active: #2e7d5e;
}

/* ── Reset ── */
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

body {
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
  font-size: 14px;
  color: var(--text);
  background: var(--bg);
}

/* ── Layout ── */
.layout {
  display: flex;
  min-height: 100vh;
}

/* ── Sidebar ── */
.sidebar {
  width: 220px;
  flex-shrink: 0;
  background: var(--sidebar-bg);
  color: var(--sidebar-text);
  display: flex;
  flex-direction: column;
  padding: 0;
  position: sticky;
  top: 0;
  height: 100vh;
  overflow-y: auto;
}

.sidebar-title {
  font-size: 15px;
  font-weight: 700;
  color: #ffffff;
  padding: 24px 20px 20px;
  border-bottom: 1px solid rgba(255,255,255,0.08);
  letter-spacing: 0.02em;
}

.stage-list {
  list-style: none;
  padding: 12px 0;
}

.stage-item {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 10px 20px;
  font-size: 13px;
  color: var(--sidebar-text);
  cursor: default;
  position: relative;
}

.stage-item.active {
  background: rgba(46, 125, 94, 0.25);
  color: var(--sidebar-active-text);
  font-weight: 600;
}

.stage-item.active a {
  color: var(--sidebar-active-text);
  text-decoration: none;
  display: flex;
  gap: 8px;
  align-items: center;
}

.stage-item.disabled {
  opacity: 0.45;
}

.stage-name {
  flex: 1;
}

.badge {
  font-size: 10px;
  background: rgba(255,255,255,0.12);
  color: var(--sidebar-text);
  padding: 1px 6px;
  border-radius: 10px;
  letter-spacing: 0.03em;
}

/* ── Main ── */
.main {
  flex: 1;
  padding: 36px 40px 60px;
  max-width: 760px;
}

.page-title {
  font-size: 22px;
  font-weight: 700;
  margin-bottom: 24px;
  color: var(--text);
}

/* ── Tabs ── */
.tab-strip {
  display: flex;
  gap: 0;
  border-bottom: 2px solid var(--border);
  margin-bottom: 24px;
}

.tab-btn {
  background: none;
  border: none;
  padding: 10px 20px;
  font-size: 14px;
  font-weight: 500;
  color: var(--text-muted);
  cursor: pointer;
  border-bottom: 2px solid transparent;
  margin-bottom: -2px;
  transition: color 0.15s, border-color 0.15s;
}

.tab-btn:hover { color: var(--text); }

.tab-btn.active {
  color: var(--tab-active);
  border-bottom-color: var(--tab-active);
  font-weight: 600;
}

/* ── Tab panels ── */
.tab-panel { display: block; }
.tab-panel.hidden { display: none; }

/* ── Cards ── */
.card {
  background: var(--card-bg);
  border: 1px solid var(--border);
  border-radius: 8px;
  padding: 20px 24px;
  margin-bottom: 16px;
}

.card-title {
  font-size: 13px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.06em;
  color: var(--text-muted);
  margin-bottom: 16px;
}

.hint {
  font-size: 12px;
  color: var(--text-muted);
  margin-bottom: 16px;
  line-height: 1.5;
}

/* ── Fields ── */
.field {
  margin-bottom: 14px;
}

.field:last-child { margin-bottom: 0; }

.field label {
  display: block;
  font-size: 13px;
  font-weight: 500;
  margin-bottom: 5px;
  color: var(--text);
}

.field label em {
  font-style: normal;
  font-weight: 400;
  color: var(--text-muted);
  font-size: 12px;
}

.input-row {
  display: flex;
  align-items: center;
  gap: 8px;
}

.input-row input {
  flex: 1;
  padding: 7px 10px;
  border: 1px solid var(--border);
  border-radius: 5px;
  font-size: 14px;
  color: var(--text);
  background: #fff;
  transition: border-color 0.15s;
  max-width: 220px;
}

.input-row input:focus {
  outline: none;
  border-color: var(--accent);
}

.input-row input.input-error {
  border-color: var(--error-color);
}

.unit {
  font-size: 12px;
  color: var(--unit-color);
  white-space: nowrap;
}

.field-error {
  display: block;
  font-size: 12px;
  color: var(--error-color);
  margin-top: 4px;
  min-height: 16px;
}

/* ── Calculate button ── */
#calc-btn {
  display: block;
  width: 100%;
  padding: 12px;
  margin-top: 20px;
  background: var(--accent);
  color: #fff;
  border: none;
  border-radius: 6px;
  font-size: 15px;
  font-weight: 600;
  cursor: pointer;
  letter-spacing: 0.02em;
  transition: background 0.15s;
}

#calc-btn:hover { background: #256b50; }
#calc-btn:active { background: #1d5540; }

/* ── Calc-level error ── */
.calc-error {
  margin-top: 12px;
  padding: 10px 14px;
  background: #fef2f2;
  border: 1px solid #fca5a5;
  border-radius: 6px;
  font-size: 13px;
  color: var(--error-color);
}

.calc-error.hidden { display: none; }

/* ── Result panel ── */
.result-panel {
  margin-top: 28px;
  background: var(--result-bg);
  border: 1px solid var(--result-border);
  border-radius: 8px;
  padding: 20px 24px;
}

.result-panel.hidden { display: none; }

.result-title {
  font-size: 13px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.06em;
  color: var(--text-muted);
  margin-bottom: 16px;
}

.result-grid {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.result-item {
  display: flex;
  justify-content: space-between;
  align-items: baseline;
  border-bottom: 1px solid var(--result-border);
  padding-bottom: 10px;
}

.result-item:last-child {
  border-bottom: none;
  padding-bottom: 0;
}

.result-label {
  font-size: 13px;
  color: var(--text-muted);
}

.result-value {
  font-size: 20px;
  font-weight: 700;
  color: #065f46;
}
```

- [ ] **Step 2: Hard-refresh the browser and verify layout**

Open `http://127.0.0.1:5000/` in a browser.
Check: two-column layout visible, dark sidebar with 7 stages, s4 highlighted, Basic/Advanced tab strip present.

- [ ] **Step 3: Commit**

```bash
git add static/style.css
git commit -m "feat: add CSS styling — sidebar, cards, tabs, result panel, errors"
```

---

### Task 4: Create `static/main.js`

**Files:**
- Create: `static/main.js`

**Interfaces:**
- Consumes: DOM IDs/classes from `templates/index.html` (Task 2), `POST /calculate` from `app.py` (Task 1)
- Produces: interactive tab switching, async form submission, result rendering, error display

- [ ] **Step 1: Create `static/main.js`**

```javascript
(function () {
  'use strict';

  // ── Tab switching ──────────────────────────────────────────────────────
  const tabBtns = document.querySelectorAll('.tab-btn');
  const tabPanels = document.querySelectorAll('.tab-panel');

  tabBtns.forEach(btn => {
    btn.addEventListener('click', () => {
      tabBtns.forEach(b => b.classList.remove('active'));
      tabPanels.forEach(p => p.classList.add('hidden'));
      btn.classList.add('active');
      document.getElementById('tab-' + btn.dataset.tab).classList.remove('hidden');
    });
  });

  // ── Helpers ───────────────────────────────────────────────────────────
  function clearErrors() {
    document.querySelectorAll('.field-error').forEach(el => { el.textContent = ''; });
    document.querySelectorAll('.input-error').forEach(el => el.classList.remove('input-error'));
    const banner = document.getElementById('calc-error');
    banner.textContent = '';
    banner.classList.add('hidden');
  }

  function showFieldError(fieldName, message) {
    const input = document.querySelector(`[name="${fieldName}"]`);
    if (input) input.classList.add('input-error');
    const errEl = document.querySelector(`.field-error[data-for="${fieldName}"]`);
    if (errEl) errEl.textContent = message;
  }

  function showCalcError(message) {
    const banner = document.getElementById('calc-error');
    banner.textContent = message;
    banner.classList.remove('hidden');
  }

  function collectInputs() {
    const fields = [
      'q_h','q_m','q_y','k','alpha','T_g','Cp','mfls','T_in_HP',
      'rbore','rpin','rpext','kgrout','kpipe','LU','hconv','B','NB','A'
    ];
    const data = {};
    fields.forEach(name => {
      const el = document.querySelector(`[name="${name}"]`);
      data[name] = el ? el.value.trim() : '';
    });
    return data;
  }

  // ── Calculate ─────────────────────────────────────────────────────────
  document.getElementById('calc-btn').addEventListener('click', async () => {
    clearErrors();
    document.getElementById('result-panel').classList.add('hidden');

    const data = collectInputs();

    let response, result;
    try {
      response = await fetch('/calculate', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(data),
      });
      result = await response.json();
    } catch (err) {
      showCalcError('Network error: ' + err.message);
      return;
    }

    if (!response.ok) {
      if (result.error === 'field') {
        showFieldError(result.field, result.message);
        // If the error field is in Advanced tab, switch to it
        const advFields = ['rbore','rpin','rpext','kgrout','kpipe','LU','hconv','Cp'];
        if (advFields.includes(result.field)) {
          document.querySelector('[data-tab="advanced"]').click();
        }
      } else {
        showCalcError(result.message || 'Calculation failed.');
      }
      return;
    }

    // ── Show results ──
    document.getElementById('res-L').textContent = result.L.toLocaleString();
    document.getElementById('res-H').textContent = result.H.toLocaleString();
    document.getElementById('res-NB').textContent = result.NB.toLocaleString();
    document.getElementById('result-panel').classList.remove('hidden');
    document.getElementById('result-panel').scrollIntoView({ behavior: 'smooth', block: 'nearest' });
  });

})();
```

- [ ] **Step 2: Full end-to-end test in the browser**

With Flask running (`venv/bin/flask --app app run`), open `http://127.0.0.1:5000/`.

Fill Basic tab with the philippe .xls Case D values:
- q_h: `12000`, q_m: `6000`, q_y: `1500`
- k: `2.0`, alpha: `0.086`, T_g: `15`
- B: `6.1`, NB: `5`, A: `1`, T_in_HP: `40.2`, mfls: `0.05`

Click **Calculate**.
Expected: result panel appears showing `L: 152 m`, `H: 30 m`, `NB: 5`.

- [ ] **Step 3: Test field error display**

Clear q_h, click **Calculate**.
Expected: red border on q_h input, red message `'q_h' is required` below it. Result panel stays hidden.

- [ ] **Step 4: Test Advanced-tab error auto-switch**

Set alpha: `0.5` (out of range), click **Calculate**.
Expected: browser stays on Basic tab, red error shown next to alpha field.

Set rbore to `0.2` (out of range), click **Calculate**.
Expected: browser automatically switches to Advanced tab and shows red error next to rbore field.

- [ ] **Step 5: Test tab switching preserves values**

Enter some values in Basic, click Advanced, enter values there, click Basic.
Expected: Basic values are still filled in (no reset on tab switch).

- [ ] **Step 6: Commit**

```bash
git add static/main.js
git commit -m "feat: add JS tab switching, fetch POST, result/error rendering"
```

---

## Self-Review

**Spec coverage:**
- ✅ `GET /` serves `index.html` — Task 1
- ✅ `POST /calculate` with JSON in/out — Task 1
- ✅ Server-side validation: all fields numeric, α range, rbore range, NB≥1, A≥1 — Task 1
- ✅ Sidebar with 7 stages, s4 active, others dimmed with "soon" badge — Task 2
- ✅ Two-tab form Basic (11 fields, 3 groups) / Advanced (8 fields, pre-filled defaults) — Task 2
- ✅ Sign convention hint under Ground Loads — Task 2
- ✅ Result panel hidden until success, shows L / H / NB — Tasks 2 & 4
- ✅ Field-level error: red border + message — Tasks 3 & 4
- ✅ Calc-level error banner — Tasks 3 & 4
- ✅ Auto-switch to Advanced tab on Advanced-field error — Task 4
- ✅ Dark sidebar, card groups, underline tabs, green result panel — Task 3
- ✅ No inline styles, no external frameworks, system font — Task 3
- ✅ Flask runnable via `venv/bin/flask --app app run` — Task 1

**Placeholder scan:** None found.

**Type consistency:** All 19 field names match between `app.py` `fields` list, HTML `name` attributes, and `main.js` `fields` array.
